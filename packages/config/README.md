# @safecity/config

Shared presets so every workspace lints/builds identically:

- `eslint-preset` — includes **`jsx-a11y`** (accessibility lint is mandatory, KB 11).
- `tsconfig` bases (extends root `tsconfig.base.json`).
- `prettier` config.
