{
  "name": "@safecity/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared eslint / tsconfig / prettier presets (incl. jsx-a11y).",
  "scripts": {
    "lint": "echo \"(scaffold) config has no lint\""
  }
}
