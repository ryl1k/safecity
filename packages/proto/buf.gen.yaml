version: v2
plugins:
  # Go message + gRPC stubs → apps/api (committed; regenerate with `buf generate`).
  - local: protoc-gen-go
    out: ../../apps/api/internal/mlpb
    opt: paths=source_relative
  - local: protoc-gen-go-grpc
    out: ../../apps/api/internal/mlpb
    opt: paths=source_relative
