{
  "name": "@safecity/mobile",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo \"(scaffold) expo start --dev-client\"",
    "lint": "echo \"(scaffold) lint mobile\"",
    "type-check": "tsc --noEmit",
    "test": "echo \"(scaffold) test mobile\"",
    "build": "echo \"(scaffold) eas build\""
  },
  "dependencies": {
    "@safecity/shared": "workspace:*",
    "@safecity/design-tokens": "workspace:*"
  }
}
