{
  "name": "@safecity/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo \"(scaffold) run api dev server\"",
    "lint": "echo \"(scaffold) lint api\"",
    "type-check": "tsc --noEmit",
    "test": "echo \"(scaffold) test api\"",
    "build": "echo \"(scaffold) build api\""
  },
  "dependencies": {
    "@safecity/shared": "workspace:*"
  }
}
